<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<title>AURUM.SYS | Trampas IoT</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --bg:#0b0f1a;
  --panel:#12172a;
  --card:#161c35;
  --accent:#22c55e;
  --text:#e5e7eb;
  --muted:#94a3b8;
}

*{box-sizing:border-box;}

body{
  margin:0;
  font-family: 'Segoe UI', Arial, sans-serif;
  background:var(--bg);
  color:var(--text);
}

/* HEADER */
header{
  background:linear-gradient(90deg,#020617,#020617);
  padding:15px 25px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  border-bottom:1px solid #1e293b;
}

header .left{
  display:flex;
  align-items:center;
  gap:15px;
}

header img{
  height:42px;
}

header h1{
  font-size:18px;
  font-weight:500;
  margin:0;
}

.status{
  display:flex;
  align-items:center;
  gap:8px;
  font-size:14px;
}

.status span{
  width:10px;
  height:10px;
  border-radius:50%;
  background:var(--accent);
}

/* MAIN */
.container{
  max-width:1200px;
  margin:auto;
  padding:25px;
}

.grid{
  display:grid;
  grid-template-columns:2fr 1fr;
  gap:20px;
}

.card{
  background:var(--card);
  border-radius:14px;
  padding:18px;
  box-shadow:0 10px 25px rgba(0,0,0,.4);
}

/* IMAGE */
.image-box img{
  width:100%;
  border-radius:12px;
  border:1px solid #1e293b;
}

.meta{
  display:flex;
  gap:20px;
  margin-top:10px;
  font-size:14px;
  color:var(--muted);
}

/* METRICS */
.metric{
  background:var(--panel);
  border-radius:12px;
  padding:15px;
  margin-bottom:15px;
}

.metric h3{
  margin:0;
  font-size:13px;
  color:var(--muted);
}

.metric p{
  margin:5px 0 0;
  font-size:22px;
}

/* TABLE */
table{
  width:100%;
  border-collapse:collapse;
  margin-top:15px;
}

table th, table td{
  padding:12px;
  border-bottom:1px solid #1e293b;
  font-size:14px;
}

table th{
  color:var(--muted);
  font-weight:500;
  text-align:left;
}

/* FOOTER */
footer{
  text-align:center;
  padding:20px;
  font-size:12px;
  color:var(--muted);
}
</style>
</head>

<body>

<header>
  <div class="left">
    <img src="logo.png" alt="Logo">
    <h1>Trampas IoT – Panel de Monitoreo</h1>
  </div>
  <div class="status">
    <span></span>
    Sistema Online
  </div>
</header>

<div class="container">

  <div class="grid">

    <!-- Última detección -->
    <div class="card">
      <h2>Última detección</h2>
      <div class="image-box">
        <img src="https://via.placeholder.com/800x500?text=Imagen+capturada" alt="Evento">
      </div>
      <div class="meta">
        <div>📅 21-12-2025</div>
        <div>⏰ 14:32</div>
        <div>📍 Trampa #1</div>
      </div>
    </div>

    <!-- Métricas -->
    <div>
      <div class="metric">
        <h3>Eventos hoy</h3>
        <p>3</p>
      </div>

      <div class="metric">
        <h3>Último evento</h3>
        <p>Hace 12 min</p>
      </div>

      <div class="metric">
        <h3>Batería</h3>
        <p>78%</p>
      </div>
    </div>

  </div>

  <!-- Historial -->
  <div class="card" style="margin-top:25px;">
    <h2>Historial de eventos</h2>
    <table>
      <thead>
        <tr>
          <th>Fecha</th>
          <th>Hora</th>
          <th>Trampa</th>
          <th>Evento</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>21-12-2025</td>
          <td>14:32</td>
          <td>#1</td>
          <td>Movimiento detectado</td>
        </tr>
        <tr>
          <td>21-12-2025</td>
          <td>11:05</td>
          <td>#2</td>
          <td>Movimiento detectado</td>
        </tr>
      </tbody>
    </table>
  </div>

</div>

<footer>
© 2025 Aurum.sys – Plataforma IoT
</footer>

</body>
</html>
